---
import { site, nav } from "../data/site";
const year = new Date().getFullYear();

const mapsHref = `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(
  `${site.contact.address}, ${site.contact.postalCode} ${site.contact.city}`
)}`;
---

<footer class="site-footer">
  <div class="container footer-grid">
    <div class="footer-brand">
      <p class="brand">Dogma</p>
      <p class="footer-tag">{site.tagline}</p>

      <p class="footer-accolade">
        <a href={site.social.tripadvisor} target="_blank" rel="noopener">
          Travellers’ Choice 2025 · Tripadvisor — Top 10% worldwide
        </a>
      </p>
    </div>

    <nav class="footer-col" aria-label="Footer navigation">
      <h4 class="footer-h">Explore</h4>
      <ul class="footer-list">
        {nav.map((i) => <li><a href={i.href}>{i.label}</a></li>)}
        <li><a href="/privacy">Privacy</a></li>
      </ul>
    </nav>

    <div class="footer-col">
      <h4 class="footer-h">Reach us</h4>
      <ul class="footer-list">
        <li><a href={mapsHref} target="_blank" rel="noopener">{site.contact.address}, {site.contact.city}</a></li>
        <li><a href={site.contact.phoneHref}>{site.contact.phone}</a></li>
        {site.contact.email && (
          <li><a href={`mailto:${site.contact.email}`}>{site.contact.email}</a></li>
        )}
        {site.social.instagram && (
          <li><a href={site.social.instagram} target="_blank" rel="noopener">{site.social.instagramHandle ?? "Instagram"}</a></li>
        )}
      </ul>
    </div>
  </div>

  <div class="container footer-bottom">
    <p>© {year} {site.name}. All rights reserved.</p>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--wine-950);
    color: var(--cream-50);
    padding-top: clamp(3rem, 6vw, 5rem);
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 1.6fr 1fr 1.2fr;
    gap: clamp(2rem, 5vw, 4rem);
    padding-bottom: clamp(2.5rem, 5vw, 4rem);
  }
  .footer-brand .brand {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 600;
    letter-spacing: 0.28em;
    text-transform: uppercase;
  }
  .footer-tag {
    margin-top: 0.9rem;
    max-width: 30ch;
    color: rgba(251, 246, 236, 0.7);
    font-family: var(--font-display);
    font-size: var(--step-1);
    line-height: 1.35;
  }
  .footer-h {
    font-family: var(--font-body);
    font-size: var(--step--1);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--gold-300);
    margin-bottom: 1.1rem;
  }
  .footer-list { display: grid; gap: 0.6rem; }
  .footer-list a {
    color: rgba(251, 246, 236, 0.82);
    transition: color 0.2s ease;
  }
  .footer-list a:hover { color: var(--gold-300); }

  /* Tripadvisor Travellers' Choice — one-line text mention */
  .footer-accolade {
    margin-top: 1.6rem;
    font-size: var(--step--1);
    line-height: 1.5;
    color: rgba(251, 246, 236, 0.6);
  }
  .footer-accolade a {
    color: rgba(251, 246, 236, 0.82);
    border-bottom: 1px solid rgba(201, 168, 106, 0.4);
    padding-bottom: 1px;
    transition: color 0.2s ease, border-color 0.2s ease;
  }
  .footer-accolade a:hover { color: var(--gold-300); border-color: var(--gold-300); }

  .footer-bottom {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.6rem;
    padding-block: 1.5rem;
    border-top: 1px solid rgba(251, 246, 236, 0.12);
    font-size: var(--step--1);
    color: rgba(251, 246, 236, 0.55);
  }
  .footer-bottom a { color: var(--gold-300); }
  .footer-bottom a:hover { color: var(--cream-50); }

  @media (max-width: 760px) {
    .footer-grid { grid-template-columns: 1fr 1fr; }
    .footer-brand { grid-column: 1 / -1; }
  }
  @media (max-width: 460px) {
    .footer-grid { grid-template-columns: 1fr; }
  }
</style>
